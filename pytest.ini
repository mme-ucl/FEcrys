[pytest]
testpaths = tests
addopts = -ra
markers =
    architecture: deterministic neural-network architecture invariant tests
    integration: tests that compose multiple FEcrys model components
    slow: tests that are unsuitable for the default fast suite
